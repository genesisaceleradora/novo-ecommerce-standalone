'use client'

import Link from 'next/link'
import { AdminStatusBadge } from '@/components/admin/AdminStatusBadge'
import { Card } from '@/components/ui/Card'
import { useMockAdminOrders } from '@/hooks/useMockAdminOrders'
import { formatPriceInBRL } from '@/lib/formatters'
import { adminOrderStatuses, adminOrderStatusLabels } from '@/types/admin'

function formatDate(value: string) {
  return new Intl.DateTimeFormat('pt-BR', { dateStyle: 'medium', timeStyle: 'short' }).format(new Date(value))
}

export function AdminOrderDetail({ orderId }: { orderId: string }) {
  const { orders, updateOrderStatus } = useMockAdminOrders()
  const order = orders.find((candidate) => candidate.id === orderId)
  if (!order) return <Card className="p-6"><p className="text-sm text-muted">Pedido demonstrativo não encontrado nesta sessão.</p><Link className="mt-4 inline-block text-sm font-semibold text-gold-dark" href="/admin">Voltar aos pedidos</Link></Card>

  return <div>
    <Link className="text-sm font-semibold text-gold-dark hover:text-navy" href="/admin">← Voltar aos pedidos</Link>
    <div className="mt-5 flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.15em] text-gold-dark">Pedido demonstrativo</p><h1 className="mt-2 font-display text-4xl font-semibold text-navy">{order.number}</h1><p className="mt-2 text-sm text-muted">Criado em {formatDate(order.createdAt)}</p></div><AdminStatusBadge status={order.status} /></div>
    <div className="mt-6 grid gap-5 lg:grid-cols-[1.5fr_0.9fr]">
      <div className="space-y-5">
        <Card className="p-5 sm:p-6"><h2 className="font-display text-2xl font-semibold text-navy">Itens e personalização</h2><div className="mt-5 space-y-5">{order.items.map((item) => <div className="border-t border-line pt-5 first:border-t-0 first:pt-0" key={item.id}><div className="flex flex-wrap justify-between gap-2"><div><h3 className="font-semibold text-navy">{item.productName}</h3><p className="mt-1 text-sm text-muted">{item.quantity} × {formatPriceInBRL(item.unitPrice)}</p></div><p className="font-semibold text-navy">{formatPriceInBRL(item.total)}</p></div>{item.customization && <div className="mt-4 rounded-xl bg-ivory p-4 text-sm"><p className="font-semibold text-navy">Resumo de personalização</p><dl className="mt-3 grid gap-2 text-muted">{item.customization.name && <div><dt className="inline font-medium text-navy">Nome: </dt><dd className="inline">{item.customization.name}</dd></div>}{item.customization.phrase && <div><dt className="inline font-medium text-navy">Frase: </dt><dd className="inline">{item.customization.phrase}</dd></div>}{item.customization.date && <div><dt className="inline font-medium text-navy">Data: </dt><dd className="inline">{item.customization.date}</dd></div>}{item.customization.notes && <div><dt className="inline font-medium text-navy">Observações: </dt><dd className="inline">{item.customization.notes}</dd></div>}{item.customization.musicLink && <div><dt className="inline font-medium text-navy">Música: </dt><dd className="inline break-all">{item.customization.musicLink}</dd></div>}</dl></div>}{item.files.length > 0 && <div className="mt-4"><p className="text-sm font-semibold text-navy">Arquivos selecionados (metadados)</p><ul className="mt-2 space-y-2">{item.files.map((file) => <li className="rounded-xl border border-line p-3 text-xs text-muted" key={file.id}><p className="font-semibold text-navy">{file.originalFileName}</p><p className="mt-1 break-all">{file.mimeType} · {Math.round(file.fileSize / 1000)} KB · {file.storagePath}</p></li>)}</ul></div>}</div>)}</div></Card>
        <Card className="p-5 sm:p-6"><h2 className="font-display text-2xl font-semibold text-navy">Histórico</h2><ol className="mt-5 space-y-4 border-l border-gold/50 pl-5">{order.history.map((entry) => <li className="relative" key={entry.id}><span className="absolute -left-[25px] top-1.5 size-2 rounded-full bg-gold" /><p className="text-sm font-semibold text-navy">{adminOrderStatusLabels[entry.status]}</p><p className="mt-1 text-sm text-muted">{entry.description}</p><p className="mt-1 text-xs text-muted">{formatDate(entry.createdAt)}</p></li>)}</ol></Card>
      </div>
      <div className="space-y-5"><Card className="p-5"><h2 className="font-display text-2xl font-semibold text-navy">Status</h2><label className="mt-4 block text-sm font-semibold text-navy" htmlFor="order-status">Produção e atendimento<select className="mt-2 w-full rounded-xl border border-line bg-cream px-3 py-3 font-normal text-ink outline-none focus:border-gold" id="order-status" onChange={(event) => updateOrderStatus(order.id, event.target.value as typeof order.status)} value={order.status}>{adminOrderStatuses.map((status) => <option key={status} value={status}>{adminOrderStatusLabels[status]}</option>)}</select></label><p className="mt-3 text-xs leading-5 text-muted">A mudança é salva somente no localStorage deste navegador.</p></Card>
        <Card className="p-5"><h2 className="font-display text-2xl font-semibold text-navy">Cliente</h2><dl className="mt-4 space-y-3 text-sm"><div><dt className="text-muted">Nome</dt><dd className="font-medium text-navy">{order.customer.fullName}</dd></div><div><dt className="text-muted">Contato</dt><dd className="font-medium text-navy">{order.customer.email}<br />{order.customer.phone}</dd></div><div><dt className="text-muted">Documento</dt><dd className="font-medium text-navy">{order.customer.document}</dd></div></dl></Card>
        <Card className="p-5"><h2 className="font-display text-2xl font-semibold text-navy">Resumo</h2><dl className="mt-4 space-y-2 text-sm"><div className="flex justify-between"><dt className="text-muted">Subtotal</dt><dd>{formatPriceInBRL(order.subtotal)}</dd></div><div className="flex justify-between"><dt className="text-muted">Entrega</dt><dd>{formatPriceInBRL(order.shippingTotal)}</dd></div><div className="flex justify-between border-t border-line pt-3 font-semibold text-navy"><dt>Total</dt><dd>{formatPriceInBRL(order.total)}</dd></div><div className="pt-2"><dt className="text-muted">Pagamento</dt><dd className="mt-1 font-medium text-navy">{order.paymentStatus}</dd></div>{order.trackingCode && <div><dt className="text-muted">Rastreio</dt><dd className="mt-1 font-medium text-navy">{order.trackingCode}</dd></div>}</dl></Card>
        {(order.customerNotes || order.internalNotes || order.utms) && <Card className="p-5"><h2 className="font-display text-2xl font-semibold text-navy">Contexto</h2>{order.customerNotes && <p className="mt-4 text-sm text-muted"><strong className="text-navy">Cliente: </strong>{order.customerNotes}</p>}{order.internalNotes && <p className="mt-3 text-sm text-muted"><strong className="text-navy">Interno: </strong>{order.internalNotes}</p>}{order.utms && <dl className="mt-4 border-t border-line pt-4 text-xs text-muted">{Object.entries(order.utms).map(([key, value]) => <div className="mt-1" key={key}><dt className="inline font-semibold text-navy">{key}: </dt><dd className="inline">{value}</dd></div>)}</dl>}</Card>}
      </div>
    </div>
  </div>
}
